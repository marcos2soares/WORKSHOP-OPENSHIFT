apiVersion: apps/v1
kind: Deployment
metadata:
 labels:
   app: appvt121170
 name: appvt121170-deploy
spec:
 replicas: 2
 selector:
   matchLabels:
     app: appvt121170
 template:
   metadata:
     labels:
       app: appvt121170
   spec:
     containers:
       - image: default-route-openshift-image-registry.apps.ocplab.vtal.intra/vt121170prj/appvt121170:1.0
         imagePullPolicy: Always
         name: appvt121170-cont
         ports:
           - containerPort: 6000
